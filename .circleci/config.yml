version: 2
jobs:
  build:
    branches:
      only:
        -master
    docker:
      - image: cirrusci/flutter
    steps:
      - checkout
      - run: flutter doctor
      - run: flutter test
      - run: flutter -v build apk --debug